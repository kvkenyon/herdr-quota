[package]
name = "herdr-quota"
version = "0.3.0"
edition = "2024"
rust-version = "1.88"
description = "Rust foundation for the Herdr quota sidebar"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "herdr-quota"
path = "src/main.rs"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["std"] }
libc = "0.2"
regex = "1.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tokio = { version = "1.47", features = ["io-util", "macros", "process", "rt", "sync", "time"] }
time = { version = "0.3", features = ["formatting", "macros", "parsing"] }

[dev-dependencies]
tempfile = "3.20"
tokio = { version = "1.47", features = ["macros", "rt-multi-thread"] }
